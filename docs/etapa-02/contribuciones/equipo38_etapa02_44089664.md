# Contribución individual -- Etapa 02
**Equipo:** 38
**Integrante:** Romero Ingrid Luana
**Fecha:** 10-09-2026

## 1. Aporte realizado
- Revisé y ajusté el Modelo Conceptual y el Modelo Relacional armado por el grupo.
- Revisé y ajusté la normalización hasta 3FN para verificar que no queden dependencias transitivas, sobre todo en `VENTA` y `DETALLE_VENTA`.

## 2. Decisiones en las que participé
- Participé en la decisión de aplicar generalización para `PERSONA`, `USUARIO` y `CLIENTE`, agrupando los datos comunes en `PERSONA`.
- Participé en la decisión de modelar `DETALLE_VENTA` como entidad débil para resolver la relación muchos a muchos entre `VENTA` y `PRODUCTO`.

## 3. Problemas o dificultades identificadas
- No tuvimos problemas graves, solo leves diferencias de criterio al definir atributos en `PERSONA`, en `PRODUCTO` y al nombrar claves.

## 4. Soluciones o propuestas realizadas
- Propuse modificar los nombres de ciertos atributos para que reflejen mejor su propósito y evitar ambigüedades.
- Validé con el grupo que `DETALLE_VENTA` lleve cantidad y precio como atributos propios y no como parte de `PRODUCTO`.

## 5. Evidencias en el repositorio
- `docs/etapa-02/modelo_relacional.png`
- `docs/etapa-02/modelo_conceptual.png`
- `docs/etapa-02/decisiones_de_diseno.md`
- Sin commits propios en esta etapa, participación por revisión grupal.

## 6. Reflexión individual
En esta etapa comprendí mejor la normalización. Al revisar y ajustar el modelo hasta 3FN entendí por qué es importante separar bien las entidades y evitar datos repetidos, y cómo eso hace que el modelo relacional quede más limpio y sin errores después.