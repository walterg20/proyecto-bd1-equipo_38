# Contribución individual -- Etapa 02
**Equipo:** 38
**Integrante:** Torres Jemina Cesia
**Fecha:** 07-09-2026

## 1. Aporte realizado
- Diagramé el Modelo Relacional del sistema a partir del Modelo Conceptual y las reglas de negocio acordados.
- Aplique las 3 reglas de normalización.

## 2. Decisiones en las que participé
- Definimos resolver la herencia/especialización de `persona` y vincularlo a `cliente` y `usuario`.
- Acordamos resolver la relación muchos a muchos entre productos y proveedores mediante la tabla intermedia `producto_proveedor`.

## 3. Problemas o dificultades identificadas
- Dificultad al representar la PK de `persona` heredada en `usuario` y `cliente` en `venta` ya que inevitablemente esta tabla debia tener el ID de cliente y usuario vendedor.

## 4. Soluciones o propuestas realizadas
- Se cambio la PK original de `usuario` (`id_persona`) por `id_persona-usuario` en la tabla `usuario` para hacerlo mas representativo. Tambien, en la tabla `cliente` por `id_persona-cliente`.

## 5. Evidencias en el repositorio
- `docs/etapa-02/modelo_relacional.png`
- Commit: `fb296f0` - "Se agrego el diagrama del modelo relacional."
- Pull Request: -

## 6. Reflexión individual
Mejoré la capacidad de aplicar las reglas de normalización al modelo relacional.